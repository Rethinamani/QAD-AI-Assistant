# ── LLM & Embeddings ───────────────────────────────────────
langchain==0.3.25
langchain-community==0.3.23
langchain-ollama==0.3.3

# ── Vector Store ───────────────────────────────────────────
chromadb

# ── Hybrid Search ──────────────────────────────────────────
rank-bm25==0.2.2
sentence-transformers==4.1.0

# ── PDF Parsing ────────────────────────────────────────────
pymupdf==1.25.5
pdfplumber==0.11.6

# ── Image OCR (figure text extraction) ─────────────────────
# Also requires the Tesseract binary on the host (not a pip package):
#   Windows: winget install UB-Mannheim.TesseractOCR
#   then set TESSERACT_CMD in .env if it is not on PATH.
pytesseract==0.3.13
pillow>=10.0.0

# ── Excel Parsing ──────────────────────────────────────────
openpyxl==3.1.5
pandas==2.3.3

# ── Audio / Video Transcription ────────────────────────────
# Also requires the ffmpeg binary on the host (not a pip package):
#   Windows: winget install Gyan.FFmpeg
# faster-whisper downloads its model weights on first use (~1.5 GB for
# "medium"); set WHISPER_MODEL=small|base in .env for faster runs.
faster-whisper==1.1.0

# ── API ────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn==0.34.2
python-multipart==0.0.20

# ── Environment & Config ───────────────────────────────────
python-dotenv==1.1.0

# ── PII Scrubbing ──────────────────────────────────────────
presidio-analyzer==2.2.356
presidio-anonymizer==2.2.356
# Presidio needs a spaCy English model. The small model with a trimmed
# pipeline is ~2-4x faster than the large one for the entity types we redact
# (config.PII_SPACY_MODEL, default en_core_web_sm). Install after pip install:
#   python -m spacy download en_core_web_sm
# The code falls back to en_core_web_lg if the small model is absent.
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# ── File Watcher ───────────────────────────────────────────
watchdog==6.0.0

# ── Async Job Queue ────────────────────────────────────────
celery==5.5.2
redis==5.2.1

# ── Database ───────────────────────────────────────────────
SQLAlchemy==2.0.40

rank-bm25==0.2.2

beautifulsoup4==4.13.4

streamlit==1.45.1

# ── Desktop App Wrapper ────────────────────────────────────
pywebview==6.2.1
pywin32==312